name: 'fixture'
version: '1.0.0'
profile: 'chinook'
model-paths: ["models"]

models:
  fixture:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
